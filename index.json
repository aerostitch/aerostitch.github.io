---
---
[
{% assign pa = site.pages | sort: 'categories' %}
{% for p in pa %}
{% if p.title %}
{
  "name": "{{ p.title }}",
  "dir": "{{ p.dir }}",
  "layout": "{{ p.layout }}",
  "path": "{{ p.path }}",
  "categories": [ "{{ p.categories | join: '", "' }}" ],
  "tags": [ "{{ p.tags | join: '", "' }}" ],
  "url": "{{ p.url }}"
}{% unless p == pa.last %},{% endunless %}
{% endif %}
{% endfor %}
]
